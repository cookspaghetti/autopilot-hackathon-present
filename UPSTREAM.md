# Upstream provenance

This directory vendors the official AutoPilot Hackathon Round 2 starter
template as normal source files so the submission works from a single clean
clone.

- Upstream: `https://github.com/digitamizers/AutoPilot-Template.git`
- Upstream branch: `main`
- Imported commit: `f0250ac73f2ec4d5974155d111a2667e68f1e377`

The upstream `.git` directory was intentionally removed when the template was
imported into the parent repository on the Round 2 implementation branch.
